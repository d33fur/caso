#include "derivative.h"

namespace derivatives {
    DerivativeCalculator::DerivativeCalculator() {}

    double DerivativeCalculator::calculate(const double x, const double h) {
        return 0;
    }
}